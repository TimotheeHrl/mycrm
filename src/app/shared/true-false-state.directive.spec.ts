import { TrueFalseStateDirective } from "./true-false-state.directive";

describe("TrueFalseStateDirective", () => {
  it("should create an instance", () => {
    const directive = new TrueFalseStateDirective();
    expect(directive).toBeTruthy();
  });
});
